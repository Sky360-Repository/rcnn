import setuptools
setuptools.setup(name='mldetector',
                 version='0.1',
                 description='ML Detector for simpletracker',
                 url='#',
                 author='UAP Paul',
                 install_requires=['torch', 'torchvision'],
                 author_email='',
                 packages=setuptools.find_packages(),
                 zip_safe=False)
